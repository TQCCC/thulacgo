language: go
cache:
  directories:
    - $HOME/.cache/go-build
    - $GOPATH/pkg/mod
    - models

go:
  - "1.13.x"

env:
  - GO111MODULE=on
  - GOPROXY=https://goproxy.cn

branches:
  only:
    - master

install:
  - wget "http://thulac.thunlp.org/source/Models_v1_v2(v1_2).zip"
  - unzip -d models -o "Models_v1_v2(v1_2).zip"

script:
  - go version && go get -v && - go test -timeout 30s github.com/Tsingxiao/thulacgo  -v -count=1

